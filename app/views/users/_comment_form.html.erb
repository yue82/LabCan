<%= form_for(@user) do |f| %>
  <%= render 'shared/error_messages' %>
  <%= f.label :comment %>
  <%= f.text_field :comment, placeholder: 'Input your comment', class: 'form-control' %>
  <%= f.submit 'Update', class: "btn btn-primary" %>
<% end %>
